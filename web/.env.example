# Required — your Anthropic API key (users don't need their own)
ANTHROPIC_API_KEY=sk-ant-...

# Optional — paste a Zapier/Make webhook URL to capture emails
# If not set, emails are logged to the console
WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/...
